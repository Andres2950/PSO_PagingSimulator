#ifndef SIMUL_WINDOW
#define SIMUL_WINDOW

#include "imgui.h"

void showSimulWindow(bool *open, int algorithm, const char *filepath,
                     ImGuiWindowFlags windowFlags);

#endif // !SIMUL_WINDOW
